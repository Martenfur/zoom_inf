///ringCheckPoint(x,y)

/*
Checks collision with all the masks ring has.
*/

for(var i=0; i<ringMaskCount() i+=1)
{
  if ringMaskGetState(i)!=0 && ringMaskCheckPoint(argument0,argument1,i)
  {return id}
}

return noone
