///layoutDestroy(layout)

/*
Destroys the layout.
*/

var _l=ds_list_size(argument0);

for(var i=0; i<_l i+=1)
{ds_list_destroy(ds_list_find_value(argument0,i))}

ds_list_destroy(argument0)

